# odekake blueprint

## Core distinction

**親密度は目的。odekakeは移動。**

親密度の変化そのものは、おでかけによってだけ起こるわけではない。
Chat、会話、作品との接触、食事、散歩、イベント参加など、さまざまな行動によって関係は変化する。

したがって `intimacy` と `odekake` は別の概念として扱う。

```text
                    PURPOSE
                 親密度の変化
                       ↑
          ┌────────────┼────────────┐
          │            │            │
        Chat        Odekake      Experience
          │            │            │
          │       移動・経由・滞在    │
          │            │            │
          └────────────┼────────────┘
                       ↓
                 関係の状態変化
```

## Intimacy

`intimacy` はシステムの目的関数の一つとして扱う。

### solo

一人の場合は「自分と世界との関係」の変化を見る。

```text
自分 ↔ 場所
自分 ↔ 作品
自分 ↔ 店
自分 ↔ 人
自分 ↔ 音楽
自分 ↔ 街
```

### date

二人の場合は「二人の共有体験による関係」の変化を見る。

```text
人A ──┐
      ├── 共有体験 ──→ 関係の変化
人B ──┘
```

## Odekake

`odekake` は、場所をまたぐ行動・移動・経由・滞在を扱う。

```text
出発
 ↓
移動
 ↓
場所
 ↓
体験
 ↓
次の場所
 ↓
帰宅
```

目的は「移動すること」そのものではなく、移動を含む体験を組み立てること。

## Chat is also an intimacy action

Chat は `odekake` ではないが、親密度を変化させる行動になり得る。

```text
Chat
 ↓
会話・質問・共有
 ↓
理解が深まる
 ↓
intimacy が変化
```

そのため将来的には、`odekake-mcp` の世界を「場所検索」だけに閉じず、**関係を変化させる行動のオーケストレーション**として捉える。

## Planner

Planner は `intimacy` を目的として参照しつつ、`odekake` の移動計画を生成する。

```text
User Goal
   ↓
目的・mode・時間・場所・予算
   ↓
Candidates
   ↓
移動可能性を評価
   ↓
体験を組み合わせる
   ↓
intimacyへの寄与を評価
   ↓
理由を説明
   ↓
Odekake Plan
   ↓
Google Calendar
```

重要なのは、**「親密度を上げるために必ず出かける」わけではない**こと。
Chatだけで関係が深まる場合もあるし、移動そのものが目的になる場合もある。

## Design principle

> 親密度は目的である。
>
> odekakeは移動である。
>
> Chatでも親密度は変化する。
>
> だから、目的と手段を同じデータ構造に押し込めない。

`date` / `solo` は同じデータモデル上のモードとして扱い、各ドメインの Researcher は「その行動・場所・作品・人が、どのような関係変化を生み得るか」という evidence を収集する。
